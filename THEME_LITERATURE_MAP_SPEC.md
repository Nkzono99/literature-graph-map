# テーマ別文献マップ — 仕様

更新日：2026-09-17

## 目的

研究テーマごとに、先行研究の比較表と論文間の関係図を作る。このリポジトリ自体をGitHubで公開し、読者が研究の違い・つながり・原著への入口を把握できるようにする。

正式な研究成果としての完全性は求めない。誤りや抜けの可能性をページに記し、指摘や新しい情報に応じて修正する。情報が揃った部分から掲載できる。

調査方法は固定しない。AIの知識・推論、検索、外部プラグイン、既存のサーベイ、手作業などを利用できる。CLIはデータの整理・生成を担い、調査そのものの手順や使用モデルを規定しない。

## 公開構成

```text
literature-graph-map/
├── README.md
├── COPYRIGHT.md
├── data/works.jsonl
├── topics/
│   └── theme/
│       ├── README.md
│       ├── topic.yaml
│       └── subtheme/
│           ├── README.md
│           └── topic.yaml
├── docs/USAGE.md
└── src/literature_graph_map/
```

共通書誌台帳とテーマ別YAMLを正本とし、表・図・索引は同じデータから生成する。公開単位はテーマで、個別論文の詳細要約ページは作らない。同じ論文を複数テーマで扱える。

親・子・関連テーマを相対リンクで結ぶ。階層は閲覧のための構成であり、論文の関係を木構造に制限しない。PDF配布庫、日次日記、汎用RAGシステムは本プロジェクトの対象外とする。

## テーマページ

1. テーマの問いと対象範囲
2. 調査日・対象期間・採用件数と短い免責文
3. 分類別の先行研究比較表
4. Mermaidによる論文間の関係図
5. 関係の説明・参考情報
6. 必要に応じた比較の読み方
7. 調査範囲・今回の更新・関連テーマ

比較表の列は以下とする。

| 列 | 内容 |
|---|---|
| ID | 共通台帳の文献ID |
| タイトル | 原題と原著へのリンク |
| 著者 | 著者一覧 |
| 年 | 代表版の出版年 |
| 掲載誌／出版社 | 判明している書誌 |
| OA・無料公開 | 利用できる公開場所と版 |
| Preprint | プレプリント等へのリンク |
| 3項目要点 | 問い、方法、判明点・適用範囲 |

要点はテーマに合わせて書く。項目ごとの出典や確認範囲は補足できるが、記録を掲載の必須条件にしない。未実施の確認を実施済みとして表示しない。

表は分類内で年・第一著者・ID順に並べ、既定では30行ごとに分ける。図は最大20ノード・30エッジで分割する。全掲載文献を表と図に含め、関係が登録されていない文献もノードとして残す。画像を使う場合はMarkdownに埋め込む。

## 書誌・公開場所・版

文献IDは `P000001` 形式で、書誌は共通台帳に1件ずつ記録する。タイトル、著者、原著へのURL、代表版ID、版一覧が必須。DOIや書誌取得元は任意とする。

版は `V1` などのIDを持つ。`kind` は出版版・著者最終稿・投稿版・不明を区別する。年、掲載誌、出版社、DOI、リポジトリID、公開場所は判明したものを記録する。同一論文の版をまとめる際の根拠記録は任意とする。

無料公開先は登録情報に基づいて表示する。API由来の情報にはその旨を添え、公開記録の再確認は必須にしない。無料で読めることと再利用の許諾は別の情報として扱う。

## 関係データと図

関係IDは `R000001` 形式。sourceは基礎・対象となる文献、targetはそれを拡張・利用・検証する文献とする。

| 種類 | 意味 |
|---|---|
| `extends` | 理論・モデル・結果の拡張 |
| `uses_method` | 手法の利用 |
| `tests` | 予測・モデルの検証 |
| `supports` | 結果の支持 |
| `challenges` | 結果への異議・反例 |
| `compares_with` | 条件・手法・結果の比較 |

両端の文献、種類、比較軸、理由を記録する。出典、参照版、確認主体・日付、条件差は任意。`basis: explicit` は文献中の言及を実線で、既定の `comparison` はマップ上の比較整理を点線で表す。`compares_with` 以外は矢印を付ける。

`candidate` は「仮」、`recheck` は「見直し予定」を添えて図と一覧に掲載する。`checked` は通常表示で、原著確認や人間レビューの認証ではない。`rejected` は正本に残し、図と一覧から除く。

原著の精読、両側の詳細出典、公開記録の再確認を、一律の掲載条件にはしない。調査を進めながら理由や参考情報を補い、判断が変われば関係を修正する。

## 編集とCLI

CLIの使い方と最小入力例は [docs/USAGE.md](docs/USAGE.md) に置く。

正本の直接編集と `import` のどちらでも更新できる。`survey`・`add`・`update` によるCrossref検索は任意の補助機能で、調査の入口をこれらに限定しない。`render` は通信やAI処理を行わず、同じ入力から同じページを生成する。

READMEは開始・終了コメントで生成領域を区切る。再生成時は領域内を置き換え、領域外の文章を維持する。ハッシュや過去の生成状態への復元操作は不要。`check` が古い生成結果を検出した場合は `render` で更新する。

項目を固定したい場合のみ `locked_fields` を使える。版更新時は既存の参照先を保ったまま見直しの目印を付け、関係の掲載は続ける。文献・版・関係IDの参照整合性や出力先の境界はCLIで検証する。

## 掲載と免責

ページには「誤りや抜けが含まれる可能性があり、指摘に応じて修正する」旨を短く記す。出典が揃っていないことや仮の判断が残っていることを、公開を止める理由にしない。

CLIの作業用キャッシュや草稿はリポジトリ外に置く。マップだけを別ディレクトリに出力する `export` は任意であり、このリポジトリの公開に必須ではない。

`check --public` は掲載対象や秘密情報・ローカルパスの混入を確認する。原文との長い文字列照合や、研究内容の自動的な正しさ判定は行わない。著作権の扱いは [COPYRIGHT.md](COPYRIGHT.md) に記す。

## エージェント向けの指示

[OpenAIの記事](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)を参考に、常時読むAGENTS.mdには目的とリポジトリ固有の情報を絞って置く。詳細は必要な場面で参照できる文書へ分ける。調査順序、使用する道具、毎回読む文書一式を固定した手順は持たせない。

文献調査の成果は、依頼された範囲の比較表・関係図・ページの更新である。コード変更の成果は、依頼した動作が使える状態になることである。確認の量と方法は変更内容に合わせて選ぶ。
