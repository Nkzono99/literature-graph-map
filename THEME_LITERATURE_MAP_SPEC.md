# テーマ別文献マップ — 仕様

更新日：2026-09-18

## 目的

研究テーマごとに文献を整理・管理し、参照しやすい一覧を作る。GitHub PagesでHTMLサイトを公開し、引用リンク付きの概説と各文献の要点から、研究の違い・つながり・原著への入口を把握できるようにする。

正式な研究成果としての完全性は求めない。誤りや抜けの可能性をページに記し、指摘や新しい情報に応じて修正する。情報が揃った部分から掲載できる。

調査方法は固定しない。AIの知識・推論、検索、外部プラグイン、既存のサーベイ、手作業などを利用できる。CLIはデータの整理・生成を担い、調査そのものの手順や使用モデルを規定しない。

## 公開構成

```text
literature-graph-map/
├── README.md                         # リポジトリの案内
├── data/works.jsonl                  # 共通書誌
├── topics/<theme>/topic.yaml         # テーマの正本
├── src/literature_graph_map/web/     # HTMLテンプレート・CSS・JavaScript
├── .github/workflows/pages.yml       # ビルドとPagesへの公開
└── _site/                           # 生成HTML。Git管理外
```

共通書誌台帳とテーマ別YAMLを正本とし、概説・文献一覧・索引は同じデータから生成する。公開単位はテーマで、個別論文の詳細要約ページは作らない。同じ論文を複数テーマで扱える。

親・子・関連テーマを相対リンクで結ぶ。索引は `navigation.parent` に沿って階層表示し、テーマ冒頭に子テーマへのリンク、パンくずに親テーマを置く。階層は閲覧のための構成であり、論文の関係を木構造に制限しない。PDF配布庫、日次日記、汎用RAGシステムは本プロジェクトの対象外とする。

上位は「プラズマの基礎過程」「数値計算手法」「月の環境」「宇宙機・プラズマ応用」にまとめる。月面帯電の下には固有の子テーマを置き、他分野にも共通するシース・二次電子放出・プラズマ膨張は基礎過程へ置く。複数分野にまたがる関係は関連テーマのリンクで補う。階層の変更だけでは既存ページのパスを変えない。

文献を持たず、公開の子テーマを持つページは分類の入口として表示する。空の文献一覧や文献数を出さず、子テーマの件数・説明・リンクを置く。索引の収録テーマ数にはこの入口を含めない。文献と子テーマを両方持つページは通常のテーマページとして表示する。

## テーマページ

白と青灰色を基調に、分類のナビゲーションと文献一覧を細い罫線で整理する。トップページはテーマの索引とし、大きな宣伝用見出しや調査作業用の操作パネルは置かない。

サイドバーのテーマツリーは全ページで共通にし、テーマ名は常にそのテーマのページへリンクする。現在のテーマを強調し、その直下に「このページ内」の目次をインデントして置く。子テーマ・概観・文献一覧への目次リンクはページ内の移動に使い、文献の分類は一覧の絞り込みから選べる。狭い画面ではサイドバーを開閉できる。

1. テーマの問いと対象範囲
2. 採用件数と子テーマへのリンク（子テーマがある場合）
3. 研究の概観（引用リンク付きの短いレビュー本文）
4. 研究の系譜（節目と短い説明、文献へのリンク）
5. 分類別の先行研究一覧
6. 文献をたどる（必要に応じた補足）
7. 関連テーマと短い免責文

概観はテーマの仕組み、主要な知見、研究同士のつながりを文章で説明する。初めて読む人が本文だけでも大筋をつかめるよう、3〜5節・1,000〜2,000字程度を目安とし、テーマに合わせて調整する。正本の `review` に見出し（`heading`）と段落（`paragraphs`）を記録する。省略でき、字数の上限は設けない。

本文の `[@P000001]` は第一著者＋年の引用リンクへ変換する。引用先は同じテーマの文献一覧に含め、書誌の訂正は引用の表示名にも反映する。引用以外はプレーンテキストとして表示する。本文から文献へ移動したときも、必要なら検索・分類の絞り込みを解除する。

各論文をページ幅いっぱいの縦並びで表示する。第一著者＋年の表示名と研究上の役割に続けて原題リンクを見出しにし、著者・掲載誌・出版社をそれぞれ独立した行にする。OA・無料公開とPreprintは同じ行にまとめる。表示項目は以下とする。

| 項目 | 内容 |
|---|---|
| 表示名 | 第一著者＋年。同じ組み合わせは `-1`、`-2` を添える |
| タイトル | 原題と原著へのリンク |
| 著者 | 著者一覧 |
| 年 | 代表版の出版年 |
| 掲載誌／出版社 | 判明している書誌 |
| OA・無料公開 | 利用できる公開場所と版 |
| Preprint | プレプリント等へのリンク |
| 3項目要点 | 問い、方法、判明点・適用範囲 |

要点はテーマに合わせ、「問い・方法・判明点」を短い3項目の箇条書きにする。各項目が1行に収まる長さを目安とし、重要な条件や適用範囲は残す。原題や要点を表示時に切り詰めず、長い項目や狭い画面では自然に折り返す。

項目ごとの出典と確認範囲は、各論文の末尾にある「出典・確認状況」へ折りたたんで表示する。記録は掲載の必須条件にしない。未実施の確認を実施済みとして表示せず、プレプリント等の版表示は折りたたみの外に残す。

一覧は分類内で年・第一著者・ID順に並べる。タイトル・著者・要点の検索、分類の絞り込み、分類内での年の昇順・降順切り替えを使える。年が不明の文献はどちらでも末尾に置き、同年の文献の順序は保つ。概説から引用先へ移動すると、該当文献の背景を強調する。全掲載文献を一覧に含める。画像を使う場合はページに埋め込む。

## 研究の系譜

概観の下に、選んだ節目を経路ごとに縦に並べる。各節目には第一著者＋年、短い見出し、何が変わったかの説明を付ける。矢印にはつながりを短文で書く。論文ブロックをクリックすると一覧へ移動し、一覧の「系譜の節目」から戻れる。絞り込みで隠れた文献は移動時に表示する。代表版がプレプリントのときは系譜でもその状態を示す。

正本の `lineage` は経路の `title` と `steps` を持つ。各stepは `paper_id`、`title`、`significance`、前段からの `connection` を記録する。最初のstepに接続文は付けない。参照する論文は同じテーマの一覧に含め、系譜内で重複させない。テーマごとに2〜6件程度を目安とし、省略もできる。

収録文献から研究の変化を編集して示す。理論の拡張、観測による制約、手法の比較を、説明と接続文で区別する。全論文のネットワークや関係ID表は生成しない。HTMLとCSSで表示し、狭い画面では経路を一列に並べる。JavaScriptなしでも読める。

## 書誌・公開場所・版

画面では `Nakazono 2025-1` のような表示名を使う。概説の引用・文献一覧で共通とし、同姓・同年の文献は台帳内のID順で数字を添える。第一著者の姓は名前の末尾（カンマ区切りなら先頭）から求め、複合姓などは任意の `citation_author` で指定する。

内部の文献IDは `P000001` 形式で、書誌は共通台帳に1件ずつ記録する。タイトル、著者、原著へのURL、代表版ID、版一覧が必須。DOIや書誌取得元は任意とする。

版は `V1` などのIDを持つ。`kind` は出版版・著者最終稿・投稿版・不明を区別する。年、掲載誌、出版社、DOI、リポジトリID、公開場所は判明したものを記録する。同一論文の版をまとめる際の根拠記録は任意とする。

無料公開先は登録情報に基づいて表示する。公開記録の再確認は必須にしない。無料で読めることと再利用の許諾は別の情報として扱う。

## 調査用の関係データ

研究同士のつながりは概説と、選んだ節目の系譜で説明する。`relations` は理由・出典・比較条件を保持する任意の調査データとし、その詳細一覧や全体図は公開ページに置かない。`lineage` は読者向けに選んだ流れを記録し、`relations` から自動生成しない。

内部の関係IDは `R000001` 形式。sourceは基礎・対象となる文献、targetはそれを拡張・利用・検証する文献とする。

| 種類 | 意味 |
|---|---|
| `extends` | 理論・モデル・結果の拡張 |
| `uses_method` | 手法の利用 |
| `tests` | 予測・モデルの検証 |
| `supports` | 結果の支持 |
| `challenges` | 結果への異議・反例 |
| `compares_with` | 条件・手法・結果の比較 |

両端の文献、種類、比較軸、理由を記録する。出典、参照版、確認主体・日付、条件差は任意。`basis: explicit` は文献中の言及、既定の `comparison` はこのマップでの比較整理を表す。

`candidate` は候補、`checked` は整理済み、`recheck` は見直し予定、`rejected` は採用しない関係の記録とする。原著確認や人間レビューの認証ではない。状態は概説の文章を自動で変更しない。

既存の `graph_label` と `view` の図設定は正本に保持できるが、公開ページの生成には使わない。

原著の精読、両側の詳細出典、公開記録の再確認を、一律の掲載条件にはしない。調査を進めながら理由や参考情報を補い、判断が変われば関係を修正する。

## 編集とCLI

CLIの使い方と最小入力例は [docs/USAGE.md](docs/USAGE.md) に置く。

正本の直接編集と `import` のどちらでも更新できる。`survey`・`add`・`update` によるCrossref検索は任意の補助機能で、調査の入口をこれらに限定しない。`render` は通信やAI処理を行わず、同じ入力から同じページを生成する。

公開ページはHTMLに統一する。`render` は `_site/` を作り直す。HTMLは直接編集せず、本文は正本、表示はHTMLテンプレート・CSSへ反映する。`check` は正本の形式と参照整合性を検証し、生成HTMLの保存は要求しない。

検索経路・検索語・更新ログ・調査の残作業・確認担当者は編集用の記録とし、公開ページにもHTMLコメントにも出さない。既存の `survey` 等へ必要に応じて残せる。公開リポジトリのYAMLは閲覧可能なので、非公開の記録はリポジトリ外に置く。

「月面での直接観測ではない」「相互検証ではない」などの念押しを定型文として付けない。基準電位、実験条件、扱う現象の範囲など、解釈を変える条件はレビュー本文や該当文献の要点へ添える。

項目を固定したい場合のみ `locked_fields` を使える。版更新時は既存の参照先を保ったまま見直しの目印を付ける。文献・版・関係IDの参照整合性や出力先の境界はCLIで検証する。

## 掲載と免責

ページには「誤りや抜けが含まれる可能性があり、指摘に応じて修正する」旨を短く記す。出典が揃っていないことや仮の判断が残っていることを、公開を止める理由にしない。

CLIの作業用キャッシュや草稿はリポジトリ外に置く。`export` は正本と生成サイトを含むスナップショットを別ディレクトリに出力する。Pagesには `_site/` のみをアップロードする。

`check --public` は掲載対象や秘密情報・ローカルパスの混入を確認する。原文との長い文字列照合や、研究内容の自動的な正しさ判定は行わない。著作権の扱いは [COPYRIGHT.md](COPYRIGHT.md) に記す。

## エージェント向けの指示

[OpenAIの記事](https://developers.openai.com/blog/rethinking-skills-and-prompts-for-gpt-6-astra)を参考に、常時読むAGENTS.mdには目的とリポジトリ固有の情報を絞って置く。詳細は必要な場面で参照できる文書へ分ける。調査順序、使用する道具、毎回読む文書一式を固定した手順は持たせない。

文献調査の成果は、依頼された範囲の概説・文献一覧の更新である。コード変更の成果は、依頼した動作が使える状態になることである。確認の量と方法は変更内容に合わせて選ぶ。

## GitHub Pages

GitHub Actionsで検証と `lgm render` を実行し、`_site/` をPagesへ公開する。CSS・JavaScriptは生成物に含め、外部スクリプトやCDNへの接続は必要としない。研究の概観・文献一覧・出典はJavaScriptなしでも読める。検索と絞り込みにはJavaScriptを使う。

リンクは相対パスにし、プロジェクトのサブパスや入れ子のテーマで使えるようにする。設置手順は [docs/USAGE.md](docs/USAGE.md) に記す。
